/*import {query} from "express-validator";

const checkPageNumber =  query('pageNumber', ).default(1)
const checkPageSize = query('pageSize', ).default(10)
const checkSortBy = query('sortBy', ).default('createdAt')
const checkSortDirection = query('sortDirection', ).default("desc")*/
